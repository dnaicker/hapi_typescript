import { grpc } from '@improbable-eng/grpc-web';
export declare type Channel = {
    address: string;
    transport?: grpc.TransportFactory;
};
export declare function createChannel(address: string, transport?: grpc.TransportFactory): Channel;
